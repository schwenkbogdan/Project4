#include "fraction.h"
#include <sstream>
#include <fstream>

Fraction::Fraction()
{
}

Fraction::Fraction(int _numerator, int _denominator)
{
	if (true)
	{

	}
	numerator = _numerator;
	denominator = _denominator;
}